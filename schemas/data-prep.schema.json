{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/Cumulocity-IoT/datapreparation-rules-template/schemas/data-prep.schema.json",
  "title": "Data Preparation rule configuration",
  "description": "Schema for rules/<rule-name>/data-prep.yaml",
  "type": "object",
  "additionalProperties": false,
  "required": ["smartFunctionFile", "input"],
  "properties": {
    "smartFunctionFile": {
      "type": "string",
      "minLength": 1,
      "description": "Relative path to the file containing the onMessage function (e.g. smartFunction.ts)."
    },
    "input": {
      "type": "object",
      "additionalProperties": false,
      "required": ["transport", "topicPattern"],
      "description": "Source/transport configuration describing which messages the rule receives.",
      "properties": {
        "transport": {
          "type": "string",
          "minLength": 1,
          "description": "Transport type. Currently only \"mqtt\" is supported."
        },
        "topicPattern": {
          "type": "string",
          "minLength": 1,
          "not": { "pattern": "\\*\\*" },
          "description": "Topic filter. Supports + (single-level) and # (multi-level) wildcards. Must not contain '**'. The scripts automatically convert + to * when sending to the platform."
        },
        "transportId": {
          "type": "string",
          "description": "Identifier for the specific transport instance (e.g. the name of the MQTT connector). When omitted, the scripts derive it from the `transport` field."
        },
        "clientIDPattern": {
          "type": "string",
          "description": "Optional device client ID filter. Supports * as a wildcard."
        }
      }
    },
    "description": {
      "type": "string",
      "description": "Human-readable description of the rule."
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of tags for organising rules."
    },
    "disabled": {
      "type": "boolean",
      "description": "Whether the rule deploys as disabled. Default: false."
    }
  }
}
